{
  "name": "wp-ai-assistant",
  "version": "1.0.0",
  "description": "AI-powered chatbot and semantic search for WordPress",
  "private": true,
  "scripts": {
    "lint:js": "eslint assets/js --ext .js",
    "lint:js:fix": "eslint assets/js --ext .js --fix",
    "format": "prettier --write \"assets/**/*.{js,css}\"",
    "format:check": "prettier --check \"assets/**/*.{js,css}\""
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
